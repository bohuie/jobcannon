<% @company = findcompany %>
<% if user_signed_in? && @user.user_id != @company.user_id %>
<%= link_to @company.fname, profile_path(@company) %>

      <%= link_to "Follow Company", {:controller => "friendships", :action => 'addcompany', :sender => @user.user_id, :receiver => @company.user_id}, class: "btn btn-small btn-primary"%>

<% end %>