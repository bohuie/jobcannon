d3.selectAll(".node").select("circle").transition().style("fill", "#ccc");
d3.selectAll(".node").select("text").transition()
 	.duration(250)
    .attr("x", -12)
    .attr("y", -12)
    .style("opacity", 1);
d3.selectAll(".node")
	.each(function(d){
		var current = d3.select(this);
		if(current.select("text").text().indexOf("<%= escape_javascript @search.query%>") > -1){
			current.select("circle").transition()
				.style("fill", "red")
				.attr("r", 8);
			highlightFriends(this);
		}
 	});
 $("#new_network_search")[0].reset()
